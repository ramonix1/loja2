# URL da API Fastify (browser dev — sempre localhost, não hostname Docker)
VITE_API_URL=http://localhost:3001

# Slug do tenant dev (enviado no header X-Tenant-Slug)
VITE_TENANT_SLUG=loja

# URL do legacy para exibir imagens (/images/...)
VITE_LEGACY_URL=http://localhost:3000
