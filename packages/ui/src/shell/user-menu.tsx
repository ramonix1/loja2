'use client';

/**
 * @deprecated Use `NavUser` dentro de `AppSidebar` (shell dashboard-01).
 * Mantido temporariamente para imports legados.
 */
export { NavUser as UserMenu, type NavUserProps as UserMenuProps } from './nav-user';
